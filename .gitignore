# Local secrets. Settings now live in ~/.apollo/config.properties; this is the
# pre-0.2 file, kept only so an existing install can migrate from it.
apollo.properties

# macOS
.DS_Store

# Build output
build/
*.o
*.d
apollo
launchApollo
