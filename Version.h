#ifndef APOLLO_VERSION_H
#define APOLLO_VERSION_H

// Set by the Makefile; this is the fallback for ad-hoc builds.
#ifndef APOLLO_VERSION
#define APOLLO_VERSION "dev"
#endif

#endif // APOLLO_VERSION_H
